 // Dichiarazione delle variabili
const nome: string = "Mario";
const eta: number = 21;
const isStudente: boolean = true;
const hobby: string[] = ["calcio", "chitarra"];

// Stampa in console
console.log(`Ciao, mi chiamo ${nome}, ho ${eta} anni e i miei hobby sono: ${hobby.join(", ")}.`);
